// AUTO-GENERATED from template.html — do not edit by hand.
export const DASHBOARD_TEMPLATE = "<meta charset=\"utf-8\">\n<title>Delt Dynasty — War Room</title>\n<style>\n:root{\n  --sans:-apple-system,BlinkMacSystemFont,\"Segoe UI\",Roboto,Helvetica,Arial,sans-serif;\n  --mono:ui-monospace,\"SF Mono\",SFMono-Regular,Menlo,Consolas,monospace;\n  /* light — \"paper war room\" */\n  --bg:#E9E2D3; --surface:#F6F1E7; --surface2:#FBF8F1; --raise:#FFFFFF;\n  --ink:#211B12; --muted:#6E6353; --faint:#93876F; --border:#D6CAB2; --border2:#E4DAC6;\n  --gold:#8F5F10; --gold-ink:#7A5210; --gold-soft:rgba(143,95,16,.13); --gold-line:rgba(143,95,16,.42);\n  --up:#1E7F52; --up-soft:rgba(30,127,82,.13);\n  --down:#BC3F37; --down-soft:rgba(188,63,55,.12);\n  --watch:#2C6AA3; --watch-soft:rgba(44,106,163,.12);\n  --shadow:0 1px 2px rgba(40,30,10,.06),0 8px 24px rgba(40,30,10,.06);\n}\n@media (prefers-color-scheme:dark){:root{\n  --bg:#131109; --surface:#1B180F; --surface2:#221E14; --raise:#262217;\n  --ink:#F1E9D7; --muted:#9E9179; --faint:#7C7059; --border:#332E20; --border2:#2A2519;\n  --gold:#E8B44E; --gold-ink:#F0C877; --gold-soft:rgba(232,180,78,.13); --gold-line:rgba(232,180,78,.4);\n  --up:#4EC791; --up-soft:rgba(78,199,145,.13);\n  --down:#E86C63; --down-soft:rgba(232,108,99,.13);\n  --watch:#64ACE2; --watch-soft:rgba(100,172,226,.13);\n  --shadow:0 1px 2px rgba(0,0,0,.4),0 10px 30px rgba(0,0,0,.35);\n}}\n:root[data-theme=\"light\"]{\n  --bg:#E9E2D3; --surface:#F6F1E7; --surface2:#FBF8F1; --raise:#FFFFFF;\n  --ink:#211B12; --muted:#6E6353; --faint:#93876F; --border:#D6CAB2; --border2:#E4DAC6;\n  --gold:#8F5F10; --gold-ink:#7A5210; --gold-soft:rgba(143,95,16,.13); --gold-line:rgba(143,95,16,.42);\n  --up:#1E7F52; --up-soft:rgba(30,127,82,.13); --down:#BC3F37; --down-soft:rgba(188,63,55,.12);\n  --watch:#2C6AA3; --watch-soft:rgba(44,106,163,.12);\n  --shadow:0 1px 2px rgba(40,30,10,.06),0 8px 24px rgba(40,30,10,.06);\n}\n:root[data-theme=\"dark\"]{\n  --bg:#131109; --surface:#1B180F; --surface2:#221E14; --raise:#262217;\n  --ink:#F1E9D7; --muted:#9E9179; --faint:#7C7059; --border:#332E20; --border2:#2A2519;\n  --gold:#E8B44E; --gold-ink:#F0C877; --gold-soft:rgba(232,180,78,.13); --gold-line:rgba(232,180,78,.4);\n  --up:#4EC791; --up-soft:rgba(78,199,145,.13); --down:#E86C63; --down-soft:rgba(232,108,99,.13);\n  --watch:#64ACE2; --watch-soft:rgba(100,172,226,.13);\n  --shadow:0 1px 2px rgba(0,0,0,.4),0 10px 30px rgba(0,0,0,.35);\n}\n*{box-sizing:border-box}\nbody{margin:0;background:var(--bg);color:var(--ink);font-family:var(--sans);\n  font-size:15px;line-height:1.5;-webkit-font-smoothing:antialiased;}\n.wrap{max-width:1200px;margin:0 auto;padding:0 20px 80px;}\na{color:inherit}\n.mono{font-family:var(--mono);font-variant-numeric:tabular-nums;}\n.kick{font-family:var(--mono);text-transform:uppercase;letter-spacing:.14em;font-size:11px;color:var(--faint);}\n\n/* header */\nheader.top{position:sticky;top:0;z-index:20;background:color-mix(in srgb,var(--bg) 88%,transparent);\n  backdrop-filter:blur(10px);border-bottom:1px solid var(--border);margin-bottom:22px;}\n.hrow{max-width:1200px;margin:0 auto;padding:16px 20px 0;display:flex;justify-content:space-between;align-items:flex-start;gap:18px;flex-wrap:wrap;}\n.brand h1{margin:0;font-size:26px;font-weight:800;letter-spacing:-.01em;line-height:1;}\n.brand h1 b{color:var(--gold);}\n.brand .sub{margin-top:7px;display:flex;gap:8px;flex-wrap:wrap;align-items:center;}\n.tag{font-family:var(--mono);font-size:11px;letter-spacing:.03em;color:var(--muted);\n  border:1px solid var(--border);border-radius:999px;padding:3px 9px;background:var(--surface);}\n.tag.live{color:var(--gold-ink);border-color:var(--gold-line);}\n.ctrls{display:flex;gap:10px;align-items:center;}\n.field label{display:block;font-family:var(--mono);text-transform:uppercase;letter-spacing:.12em;font-size:9.5px;color:var(--faint);margin-bottom:4px;}\nselect,input[type=search]{font-family:var(--sans);font-size:13.5px;color:var(--ink);background:var(--surface2);\n  border:1px solid var(--border);border-radius:9px;padding:8px 11px;outline:none;}\nselect{cursor:pointer;min-width:190px;font-weight:600;}\n.teambadge{display:flex;align-items:center;gap:8px;background:var(--gold-soft);border:1px solid var(--gold-line);\n  border-radius:9px;padding:7px 12px;min-width:190px;}\n.teambadge .tn{font-weight:800;font-size:14px;color:var(--gold-ink);letter-spacing:-.01em;}\n.teambadge .to{font-family:var(--mono);font-size:10.5px;color:var(--muted);}\nselect:focus,input:focus{border-color:var(--gold-line);box-shadow:0 0 0 3px var(--gold-soft);}\n.iconbtn{width:38px;height:38px;border:1px solid var(--border);background:var(--surface2);border-radius:9px;\n  cursor:pointer;color:var(--ink);font-size:16px;display:grid;place-items:center;}\n.iconbtn:hover{border-color:var(--gold-line);}\n\n/* tabs */\nnav.tabs{max-width:1200px;margin:14px auto 0;padding:0 20px;display:flex;gap:2px;flex-wrap:wrap;}\n.tab{appearance:none;border:0;background:transparent;color:var(--muted);font-family:var(--sans);\n  font-weight:700;font-size:13.5px;letter-spacing:.01em;padding:11px 15px 13px;cursor:pointer;\n  border-bottom:2px solid transparent;position:relative;top:1px;}\n.tab:hover{color:var(--ink);}\n.tab.active{color:var(--ink);border-bottom-color:var(--gold);}\n.tab .n{font-family:var(--mono);font-size:10.5px;color:var(--faint);margin-left:6px;}\n\n.view{display:none;animation:fade .25s ease;}\n.view.active{display:block;}\n@keyframes fade{from{opacity:0;transform:translateY(4px)}to{opacity:1;transform:none}}\n@media (prefers-reduced-motion:reduce){.view,*{animation:none!important;}}\n\nsection{margin-top:26px;}\n.sechead{display:flex;align-items:baseline;justify-content:space-between;gap:12px;margin-bottom:12px;flex-wrap:wrap;}\n.sechead h2{margin:0;font-size:15px;font-weight:800;letter-spacing:.02em;text-transform:uppercase;}\n.sechead .note{font-size:12.5px;color:var(--muted);}\n\n/* movers grid */\n.movers{display:grid;grid-template-columns:repeat(3,1fr);gap:14px;}\n@media(max-width:820px){.movers{grid-template-columns:1fr;}}\n.mcol{background:var(--surface);border:1px solid var(--border);border-radius:14px;padding:6px;box-shadow:var(--shadow);}\n.mcol h3{margin:0;padding:12px 12px 10px;font-size:12px;letter-spacing:.06em;text-transform:uppercase;\n  display:flex;align-items:center;gap:8px;font-weight:800;}\n.dot{width:9px;height:9px;border-radius:50%;display:inline-block;}\n.up .dot{background:var(--up)}.down .dot{background:var(--down)}.watch .dot{background:var(--watch)}\n.up h3{color:var(--up)}.down h3{color:var(--down)}.watch h3{color:var(--watch)}\n.mitem{padding:10px 12px;border-top:1px solid var(--border2);border-left:2px solid transparent;}\n.up .mitem{border-left-color:var(--up)}.down .mitem{border-left-color:var(--down)}.watch .mitem{border-left-color:var(--watch)}\n.mitem .pl{display:flex;justify-content:space-between;gap:8px;align-items:baseline;}\n.mitem .nm{font-weight:700;font-size:13.5px;}\n.mitem .meta{font-family:var(--mono);font-size:10.5px;color:var(--faint);white-space:nowrap;}\n.mitem .txt{font-size:12px;color:var(--muted);margin-top:3px;line-height:1.42;}\n.own{font-family:var(--mono);font-size:9px;letter-spacing:.05em;text-transform:uppercase;padding:2px 6px;border-radius:5px;border:1px solid var(--border);color:var(--faint);white-space:nowrap;}\n.own.fa{color:var(--up);border-color:color-mix(in srgb,var(--up) 40%,var(--border));background:var(--up-soft);}\n.own.mine{color:var(--gold-ink);border-color:var(--gold-line);background:var(--gold-soft);}\n\n/* controls */\n.controls{display:flex;gap:10px;align-items:center;flex-wrap:wrap;margin-bottom:14px;\n  background:var(--surface);border:1px solid var(--border);border-radius:12px;padding:10px 12px;box-shadow:var(--shadow);}\n.chips{display:flex;gap:4px;}\n.chip{appearance:none;border:1px solid var(--border);background:var(--surface2);color:var(--muted);\n  font-family:var(--mono);font-size:11.5px;font-weight:600;letter-spacing:.03em;padding:6px 11px;border-radius:8px;cursor:pointer;}\n.chip:hover{color:var(--ink);}\n.chip.on{background:var(--gold);border-color:var(--gold);color:#1a1305;}\n:root[data-theme=\"light\"] .chip.on,@media(prefers-color-scheme:light){.chip.on{color:#fff;}}\n.toggle{display:inline-flex;align-items:center;gap:7px;font-size:12.5px;color:var(--muted);cursor:pointer;user-select:none;}\n.toggle input{appearance:none;width:34px;height:19px;border-radius:999px;background:var(--border);position:relative;cursor:pointer;transition:.15s;}\n.toggle input:checked{background:var(--gold);}\n.toggle input::after{content:\"\";position:absolute;top:2px;left:2px;width:15px;height:15px;border-radius:50%;background:var(--raise);transition:.15s;}\n.toggle input:checked::after{left:17px;}\n.grow{flex:1 1 160px;min-width:140px;}\n.grow input{width:100%;}\n\n/* table */\n.tablewrap{overflow-x:auto;border:1px solid var(--border);border-radius:14px;background:var(--surface);box-shadow:var(--shadow);}\ntable{width:100%;border-collapse:collapse;font-size:13.5px;min-width:720px;}\nthead th{position:sticky;top:0;background:var(--surface2);text-align:left;font-family:var(--mono);\n  text-transform:uppercase;letter-spacing:.07em;font-size:10px;color:var(--faint);font-weight:600;\n  padding:11px 12px;border-bottom:1px solid var(--border);white-space:nowrap;}\nthead th.sortable{cursor:pointer;}thead th.sortable:hover{color:var(--ink);}\nthead th .ar{opacity:.5;font-size:9px;}\ntbody td{padding:10px 12px;border-bottom:1px solid var(--border2);vertical-align:top;}\ntbody tr:last-child td{border-bottom:0;}\ntbody tr:hover{background:var(--surface2);}\ntr.mine{background:var(--gold-soft);}\ntr.mine:hover{background:color-mix(in srgb,var(--gold-soft) 70%,var(--surface2));}\n.rk{font-family:var(--mono);color:var(--faint);font-size:12px;}\n.pname{font-weight:700;}\n.psub{font-family:var(--mono);font-size:10.5px;color:var(--faint);}\ntd.age,td.rd{font-family:var(--mono);font-variant-numeric:tabular-nums;color:var(--muted);white-space:nowrap;}\n.pos{font-family:var(--mono);font-size:10px;font-weight:700;letter-spacing:.03em;padding:2px 6px;border-radius:5px;border:1px solid var(--border);}\n.pos.QB{color:#c0533f}.pos.RB{color:#2f8f5b}.pos.WR{color:#2c6aa3}.pos.TE{color:#8a5fc0}\n@media(prefers-color-scheme:dark){.pos.QB{color:#e8836f}.pos.RB{color:#5cc88c}.pos.WR{color:#6fb0e6}.pos.TE{color:#b78ee6}}\n:root[data-theme=\"dark\"] .pos.QB{color:#e8836f}:root[data-theme=\"dark\"] .pos.RB{color:#5cc88c}\n:root[data-theme=\"dark\"] .pos.WR{color:#6fb0e6}:root[data-theme=\"dark\"] .pos.TE{color:#b78ee6}\n.pill{font-family:var(--mono);font-size:9.5px;font-weight:700;letter-spacing:.04em;text-transform:uppercase;padding:3px 7px;border-radius:6px;white-space:nowrap;border:1px solid transparent;}\n.pill.elite{color:var(--gold-ink);background:var(--gold-soft);border-color:var(--gold-line);}\n.pill.t1{color:var(--gold-ink);background:var(--gold-soft);}\n.pill.t2{color:var(--watch);background:var(--watch-soft);}\n.pill.t3{color:var(--muted);background:var(--surface2);border-color:var(--border);}\n.pill.depth{color:var(--faint);background:var(--surface2);border-color:var(--border2);}\n.note{color:var(--muted);font-size:12.5px;line-height:1.42;max-width:52ch;}\n.ownfull{font-family:var(--mono);font-size:11px;color:var(--muted);white-space:nowrap;}\n.ownfull.fa{color:var(--up);font-weight:700;}\n.ownfull.mine{color:var(--gold-ink);font-weight:700;}\n.flag{display:inline-block;font-family:var(--mono);font-size:8.5px;font-weight:700;letter-spacing:.05em;color:var(--down);border:1px solid color-mix(in srgb,var(--down) 40%,var(--border));background:var(--down-soft);padding:1px 4px;border-radius:4px;margin-left:6px;vertical-align:middle;}\n.sig{display:inline-flex;align-items:center;gap:4px;font-family:var(--mono);font-size:9.5px;font-weight:800;letter-spacing:.05em;padding:3px 7px;border-radius:6px;border:1px solid transparent;white-space:nowrap;cursor:default;}\n.sig .ico{font-size:9px;}\n.sig.buy{color:var(--up);background:var(--up-soft);border-color:color-mix(in srgb,var(--up) 38%,var(--border));}\n.sig.hold{color:var(--gold-ink);background:var(--gold-soft);border-color:var(--gold-line);}\n.sig.sell{color:var(--down);background:var(--down-soft);border-color:color-mix(in srgb,var(--down) 38%,var(--border));}\n.sig.na{color:var(--faint);background:var(--surface2);border-color:var(--border2);font-weight:600;}\n.sig.divided{border-style:dashed;}\n.consdot{display:inline-block;width:5px;height:5px;border-radius:50%;margin-left:1px;}\n.rsent{display:flex;align-items:center;gap:7px;font-size:11px;color:var(--muted);}\n.rsent .lab{font-family:var(--mono);font-size:9px;letter-spacing:.08em;text-transform:uppercase;color:var(--faint);}\n\n/* banners / cards */\n.banner{background:var(--surface);border:1px solid var(--border);border-left:3px solid var(--gold);border-radius:12px;padding:14px 16px;box-shadow:var(--shadow);}\n.banner h3{margin:0 0 4px;font-size:13.5px;}\n.banner p{margin:0;color:var(--muted);font-size:13px;}\n.cards{display:grid;grid-template-columns:repeat(auto-fit,minmax(150px,1fr));gap:12px;}\n.card{background:var(--surface);border:1px solid var(--border);border-radius:12px;padding:14px;box-shadow:var(--shadow);}\n.card .lab{font-family:var(--mono);text-transform:uppercase;letter-spacing:.1em;font-size:9.5px;color:var(--faint);}\n.card .big{font-family:var(--mono);font-size:26px;font-weight:700;margin-top:4px;letter-spacing:-.02em;}\n.card .big small{font-size:13px;color:var(--muted);font-weight:400;}\n.card .sub2{font-size:11.5px;color:var(--muted);margin-top:3px;}\n.two{display:grid;grid-template-columns:1fr 1fr;gap:16px;}\n@media(max-width:820px){.two{grid-template-columns:1fr;}}\n.empty{color:var(--faint);font-size:13px;padding:22px;text-align:center;}\n.needbar{display:flex;height:7px;border-radius:4px;overflow:hidden;margin-top:8px;background:var(--border2);}\n.needbar i{display:block;height:100%;}\n\n.clab{font-family:var(--mono);text-transform:uppercase;letter-spacing:.1em;font-size:9.5px;color:var(--faint);}\n.planner{display:grid;grid-template-columns:repeat(auto-fit,minmax(270px,1fr));gap:14px;}\n.pcard{background:var(--surface);border:1px solid var(--border);border-radius:14px;padding:0;box-shadow:var(--shadow);overflow:hidden;}\n.phead{display:flex;align-items:baseline;justify-content:space-between;padding:12px 14px;background:var(--surface2);border-bottom:1px solid var(--border);}\n.phead .ppick{font-family:var(--mono);font-weight:800;font-size:20px;color:var(--gold-ink);letter-spacing:-.02em;}\n.phead .poverall{font-family:var(--mono);font-size:10px;text-transform:uppercase;letter-spacing:.08em;color:var(--faint);}\n.ptarget{padding:10px 14px 4px;font-size:12.5px;color:var(--muted);}\n.ptarget b{color:var(--ink);font-size:14px;}\n.pcands{padding:6px 8px 10px;display:flex;flex-direction:column;gap:2px;}\n.pcand{display:flex;align-items:center;gap:7px;padding:6px 6px;border-radius:8px;font-size:12.5px;border-left:2px solid transparent;flex-wrap:wrap;}\n.pcand.fall{border-left-color:var(--up);background:var(--up-soft);}\n.pcand.value{border-left-color:var(--gold);}\n.pcand.reach{opacity:.72;}\n.pcand .pcrank{font-family:var(--mono);font-size:10px;color:var(--faint);width:26px;flex:none;}\n.pcand .pcname{font-weight:700;}\n.pcand .pcslot{font-family:var(--mono);font-size:10px;color:var(--gold-ink);}\n.pcand .pcrel{font-family:var(--mono);font-size:9px;text-transform:uppercase;letter-spacing:.05em;color:var(--faint);margin-left:auto;}\n.fitbadge{font-family:var(--mono);font-size:8.5px;font-weight:800;letter-spacing:.04em;color:var(--watch);background:var(--watch-soft);border:1px solid color-mix(in srgb,var(--watch) 40%,var(--border));padding:1px 5px;border-radius:5px;}\n.needline{font-size:12px;color:var(--muted);margin:2px 2px 14px;}\n.needline b{color:var(--down);}\n.rookgrid{display:grid;grid-template-columns:repeat(auto-fill,minmax(340px,1fr));gap:14px;}\n.rcard{background:var(--surface);border:1px solid var(--border);border-radius:14px;padding:14px 15px;box-shadow:var(--shadow);\n  border-top:3px solid var(--border);display:flex;flex-direction:column;gap:9px;}\n.rcard.p-boom{border-top-color:var(--up);}\n.rcard.p-var{border-top-color:#d98a2b;}\n.rcard.p-bal{border-top-color:var(--watch);}\n.rcard.p-safe{border-top-color:var(--muted);}\n.rcard.p-bust{border-top-color:var(--down);}\n.rcard .rtop{display:flex;justify-content:space-between;align-items:flex-start;gap:10px;}\n.rcard .rname{font-weight:800;font-size:16px;letter-spacing:-.01em;line-height:1.1;}\n.rcard .rmeta{font-family:var(--mono);font-size:10.5px;color:var(--faint);margin-top:3px;}\n.rpick{text-align:right;flex:none;}\n.rpick .rr{font-family:var(--mono);font-weight:800;font-size:17px;color:var(--gold-ink);line-height:1;}\n.rpick .rt{font-family:var(--mono);font-size:8.5px;letter-spacing:.05em;text-transform:uppercase;color:var(--faint);margin-top:3px;}\n.prof{align-self:flex-start;font-family:var(--mono);font-size:9.5px;font-weight:800;letter-spacing:.06em;text-transform:uppercase;padding:3px 9px;border-radius:6px;border:1px solid transparent;}\n.prof.p-boom{color:var(--up);background:var(--up-soft);border-color:color-mix(in srgb,var(--up) 40%,var(--border));}\n.prof.p-var{color:#c47518;background:rgba(217,138,43,.13);border-color:rgba(217,138,43,.4);}\n.prof.p-bal{color:var(--watch);background:var(--watch-soft);border-color:color-mix(in srgb,var(--watch) 40%,var(--border));}\n.prof.p-safe{color:var(--muted);background:var(--surface2);border-color:var(--border);}\n.prof.p-bust{color:var(--down);background:var(--down-soft);border-color:color-mix(in srgb,var(--down) 40%,var(--border));}\n@media(prefers-color-scheme:dark){.prof.p-var{color:#e8a24e;}.rcard.p-var{border-top-color:#e8a24e;}}\n:root[data-theme=\"dark\"] .prof.p-var{color:#e8a24e;}:root[data-theme=\"dark\"] .rcard.p-var{border-top-color:#e8a24e;}\n.cf{display:flex;gap:8px;font-size:12.5px;line-height:1.4;}\n.cf .cfi{font-family:var(--mono);font-weight:700;flex:none;width:16px;}\n.cf.up .cfi{color:var(--up);}.cf.dn .cfi{color:var(--down);}\n.cf .cft{color:var(--ink);}\n.rbuzz{font-size:12px;color:var(--muted);line-height:1.45;border-top:1px solid var(--border2);padding-top:8px;}\n.rbuzz b{color:var(--gold-ink);font-weight:700;}\n.rcomp{font-family:var(--mono);font-size:10px;color:var(--faint);}\nfooter{margin-top:40px;padding-top:18px;border-top:1px solid var(--border);color:var(--faint);font-size:11.5px;line-height:1.6;}\nfooter b{color:var(--muted);}\n.hl{color:var(--gold-ink);}\n</style>\n\n<header class=\"top\">\n  <div class=\"hrow\">\n    <div class=\"brand\">\n      <div class=\"kick\">Dynasty Command · Sleeper-linked</div>\n      <h1>DELT <b>DYNASTY</b> — War Room</h1>\n      <div class=\"sub\" id=\"leagueSub\"></div>\n    </div>\n    <div class=\"ctrls\">\n      <div class=\"field\">\n        <label>Your team</label>\n        <div class=\"teambadge\" id=\"teamBadge\"></div>\n      </div>\n      <div class=\"field\">\n        <label>&nbsp;</label>\n        <button class=\"iconbtn\" id=\"themeBtn\" title=\"Toggle light / dark\">◐</button>\n      </div>\n    </div>\n  </div>\n  <nav class=\"tabs\" id=\"tabs\">\n    <button class=\"tab active\" data-view=\"war\">War Room<span class=\"n\" id=\"nBoard\"></span></button>\n    <button class=\"tab\" data-view=\"rookies\">Rookie Draft<span class=\"n\" id=\"nRook\"></span></button>\n    <button class=\"tab\" data-view=\"trades\">Trades</button>\n    <button class=\"tab\" data-view=\"week\">This Week</button>\n    <button class=\"tab\" data-view=\"roster\">My Roster</button>\n  </nav>\n</header>\n\n<div class=\"wrap\">\n\n  <!-- ============ WAR ROOM ============ -->\n  <div class=\"view active\" id=\"view-war\">\n    <section>\n      <div class=\"sechead\"><h2>Today&rsquo;s Movers</h2><span class=\"note\"><span class=\"hl\">Live</span> — most-<b style=\"color:var(--up)\">added</b> &amp; most-<b style=\"color:var(--down)\">dropped</b> across Sleeper (48h), annotated with dynasty value; <b style=\"color:var(--watch)\">Watch</b> = your injured players</span></div>\n      <div class=\"movers\" id=\"movers\"></div>\n    </section>\n    <section>\n      <div class=\"sechead\"><h2>Dynasty Board — Superflex</h2><span class=\"note\">Analyst column = FantasyPros/DLF consensus: <b style=\"color:var(--up)\">Buy</b> · <b style=\"color:var(--gold-ink)\">Hold</b> · <b style=\"color:var(--down)\">Sell</b> — arrow = value trend, dot = agreement (green strong / gold mixed / red divided). Hover for the take.</span></div>\n      <div class=\"sechead\" style=\"margin-top:-6px;\"><span class=\"note\" id=\"boardCount\"></span></div>\n      <div class=\"controls\">\n        <div class=\"chips\" id=\"posChips\"></div>\n        <span class=\"clab\" style=\"margin-left:4px;\">Analyst</span><div class=\"chips\" id=\"sentChips\"></div>\n        <label class=\"toggle\"><input type=\"checkbox\" id=\"tAvail\"> Available only</label>\n        <label class=\"toggle\"><input type=\"checkbox\" id=\"tScout\"> Include rostered (scouting)</label>\n        <div class=\"grow\"><input type=\"search\" id=\"search\" placeholder=\"Search player, team, note…\"></div>\n      </div>\n      <div class=\"tablewrap\">\n        <table id=\"boardTbl\">\n          <thead><tr>\n            <th class=\"sortable\" data-k=\"rank\">#<span class=\"ar\"></span></th>\n            <th>Player</th><th>Pos</th><th>NFL</th>\n            <th class=\"sortable age\" data-k=\"age\">Age<span class=\"ar\"></span></th>\n            <th class=\"sortable\" data-k=\"tier\">Tier<span class=\"ar\"></span></th>\n            <th class=\"sortable\" data-k=\"sent\">Analyst<span class=\"ar\"></span></th>\n            <th class=\"sortable\" data-k=\"owner\">Status<span class=\"ar\"></span></th>\n            <th>Outlook</th>\n          </tr></thead>\n          <tbody id=\"boardBody\"></tbody>\n        </table>\n      </div>\n    </section>\n  </div>\n\n  <!-- ============ ROOKIES ============ -->\n  <div class=\"view\" id=\"view-rookies\">\n    <section>\n      <div class=\"sechead\"><h2>Best Available <span class=\"n\" id=\"bpaCount\"></span></h2><span class=\"note\" id=\"bpaNote\">Live board — updates as picks come off. Analyst take = <span class=\"hl\">FantasyPros</span> news + consensus, plus dynasty buzz.</span></div>\n      <div id=\"bpaList\" class=\"rookgrid\"></div>\n    </section>\n    <section>\n      <div class=\"sechead\"><h2>Pick Planner — your picks</h2><span class=\"note\">★ = best available · <span style=\"color:var(--up)\">could fall</span> / <span style=\"color:var(--gold-ink)\">value</span> / <span style=\"color:var(--muted)\">reach</span> · blue badge = fills a roster need</span></div>\n      <div class=\"controls\" style=\"align-items:center;\"><span class=\"note\" id=\"plannerMeta\"></span></div>\n      <div class=\"planner\" id=\"plannerCards\"></div>\n    </section>\n    <section>\n      <div class=\"banner\" style=\"margin-bottom:18px;\">\n        <h3>Your 2026 rookie draft war room — <span class=\"hl\">4 rounds, pre-draft</span></h3>\n        <p>Every player is <b>unrostered</b> in Delt Dynasty — your live draft board. Big number = where to aim (consensus SF rookie-draft slot, 1.01 = first pick). Each card carries a <b>boom/bust profile</b>, its <span style=\"color:var(--up);\">▲ ceiling</span> and <span style=\"color:var(--down);\">▼ risk</span>, and the freshest analyst + social buzz. Filter by profile to hunt upside swings or safe picks.</p>\n      </div>\n      <div class=\"controls\" style=\"flex-direction:column;align-items:stretch;gap:8px;\">\n        <div style=\"display:flex;gap:10px;align-items:center;flex-wrap:wrap;\">\n          <span class=\"clab\">Position</span><div class=\"chips\" id=\"rposChips\"></div>\n          <div class=\"grow\"><input type=\"search\" id=\"rsearch\" placeholder=\"Search rookie, team, college, buzz…\"></div>\n        </div>\n        <div style=\"display:flex;gap:10px;align-items:center;flex-wrap:wrap;\">\n          <span class=\"clab\">Profile</span><div class=\"chips\" id=\"rprofChips\"></div>\n        </div>\n      </div>\n      <div id=\"rookGrid\" class=\"rookgrid\"></div>\n    </section>\n  </div>\n\n  <!-- ============ TRADES ============ -->\n  <div class=\"view\" id=\"view-trades\">\n    <section>\n      <div class=\"sechead\"><h2>Trade Calculator</h2><span class=\"note\">Values from <span class=\"hl\">FantasyPros superflex dynasty ECR</span>. Add players &amp; your rookie picks to each side — <b style=\"color:var(--up)\">green</b> means you win value.</span></div>\n      <div class=\"two\">\n        <div class=\"card\" id=\"tside_give\"><div class=\"lab\" style=\"display:flex;justify-content:space-between;\">You give <span class=\"mono tsum\" style=\"color:var(--ink);font-size:14px;\">0</span></div>\n          <input list=\"assetList\" class=\"tadd\" data-side=\"give\" placeholder=\"Add player or pick…\" style=\"width:100%;margin:8px 0;\">\n          <div class=\"tlist\"></div></div>\n        <div class=\"card\" id=\"tside_get\"><div class=\"lab\" style=\"display:flex;justify-content:space-between;\">You get <span class=\"mono tsum\" style=\"color:var(--ink);font-size:14px;\">0</span></div>\n          <input list=\"assetList\" class=\"tadd\" data-side=\"get\" placeholder=\"Add player or pick…\" style=\"width:100%;margin:8px 0;\">\n          <div class=\"tlist\"></div></div>\n      </div>\n      <datalist id=\"assetList\"></datalist>\n      <div id=\"tradeVerdict\" class=\"banner\" style=\"margin-top:14px;\"><p>Add assets to both sides to see the verdict.</p></div>\n    </section>\n    <section>\n      <div class=\"sechead\"><h2>Trade Targets</h2><span class=\"note\">Players on <b>other rosters</b> that fill your needs, by dynasty value — and who to ask. Buy-window angle for your build.</span></div>\n      <div class=\"controls\"><span class=\"clab\">Position</span><div class=\"chips\" id=\"ttPosChips\"></div>\n        <div class=\"grow\"><input type=\"search\" id=\"ttSearch\" placeholder=\"Search targets…\"></div></div>\n      <div class=\"tablewrap\"><table style=\"min-width:560px;\"><thead><tr><th>Value</th><th>Player</th><th>Pos</th><th>NFL</th><th>Held by</th></tr></thead><tbody id=\"ttBody\"></tbody></table></div>\n    </section>\n  </div>\n\n  <!-- ============ THIS WEEK ============ -->\n  <div class=\"view\" id=\"view-week\">\n    <section>\n      <div class=\"banner\" id=\"weekBanner\" style=\"margin-bottom:18px;\">\n        <h3>Offseason mode — season starts soon</h3>\n        <p>Once Week 1 kicks off, this tab becomes your weekly brain: <b>Start / Sit</b> calls for your lineup and <b>waiver-wire adds</b>, driven by news + analyst chatter. For now it&rsquo;s pre-camp: who on your roster is trending, and who&rsquo;s available to stash.</p>\n      </div>\n      <div class=\"two\">\n        <div>\n          <div class=\"sechead\"><h2>Your roster — trending</h2><span class=\"note\">Start/Sit engine (in-season)</span></div>\n          <div id=\"weekStart\"></div>\n        </div>\n        <div>\n          <div class=\"sechead\"><h2>Waiver-wire targets</h2><span class=\"note\">Available adds</span></div>\n          <div id=\"weekWaiver\"></div>\n        </div>\n      </div>\n    </section>\n  </div>\n\n  <!-- ============ MY ROSTER ============ -->\n  <div class=\"view\" id=\"view-roster\">\n    <section id=\"rosterSection\"></section>\n  </div>\n\n  <footer>\n    <div><b>Sources:</b> Sleeper API (live rosters, league <span class=\"mono\">1313673066445819904</span>) · FantasyPros, DynastyLeagueFootball, DynastyNerds, KeepTradeCut, PFF &amp; r/DynastyFF (July 2026 camp reporting). Rankings are a synthesis, not a single canonical list — verify depth charts before trades.</div>\n    <div style=\"margin-top:8px;\"><b>Live:</b> board, waivers &amp; rookie ranks are anchored to the <span class=\"hl\">FantasyPros dynasty superflex ECR</span> feed (consensus rank + 30-day movement), refreshed automatically on each visit. <b id=\"updated\"></b>.</div>\n  </footer>\n</div>\n\n<script>window.DASH = /*__DATA__*/{};</script>\n<script>\nconst D = window.DASH;\nconst $ = s=>document.querySelector(s), $$=s=>[...document.querySelectorAll(s)];\nconst teamsById = Object.fromEntries(D.teams.map(t=>[t.id,t]));\nconst TIER_ORD = {'Elite':0,'Tier 1':1,'Tier 2':2,'Tier 3':3,'Depth':4};\nconst TIER_CLS = {'Elite':'elite','Tier 1':'t1','Tier 2':'t2','Tier 3':'t3','Depth':'depth'};\nconst esc = s => (s==null?'':String(s)).replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]));\n\nconst MY = D.myTeam;   // locked to the owner's roster\nlet state = {\n  pos:'ALL', rpos:'ALL', rprof:'ALL', sent:'ALL', search:'', rsearch:'',\n  avail:false, scout:false, sortK:'rank', sortDir:1,\n  ttPos:'ALL', ttSearch:'',\n  slot: (D.draft&&D.draft.slot)||1, dtype: (D.draft&&D.draft.type)||'linear'\n};\nconst PROF_CLS = {'Boom':'p-boom','High-variance':'p-var','Balanced':'p-bal','Safe floor':'p-safe','Bust risk':'p-bust'};\nconst SIG_CLS = {'Buy':'buy','Hold':'hold','Sell':'sell'};\nconst SIG_ORD = {'Buy':0,'Hold':1,'Sell':2};\nconst TREND_ICO = {'rising':'▲','steady':'—','falling':'▼'};\nconst CONS_COL = {'strong':'var(--up)','mixed':'var(--gold)','divided':'var(--down)'};\nfunction sigPill(s){\n  if(!s) return `<span class=\"sig na\">—</span>`;\n  const cls=SIG_CLS[s.signal]||'hold';\n  const div=s.consensus==='divided'?' divided':'';\n  const tip=`${s.signal} · ${s.trend} · analysts ${s.consensus}${s.src?' ('+s.src+')':''} — ${s.take||''}`;\n  return `<span class=\"sig ${cls}${div}\" title=\"${esc(tip)}\"><span class=\"ico\">${TREND_ICO[s.trend]||''}</span>${esc(s.signal)}<span class=\"consdot\" style=\"background:${CONS_COL[s.consensus]||'var(--muted)'}\"></span></span>`;\n}\n/* FantasyPros 30-day ECR movement chip (negative delta = rising in consensus) */\nfunction fpMove(d){\n  if(d==null || Math.abs(d)<1) return '';\n  return d<0 ? `<span style=\"color:var(--up)\" title=\"Up ${Math.round(-d)} spots in FantasyPros consensus (30d)\">▲${Math.round(-d)}</span>`\n             : `<span style=\"color:var(--down)\" title=\"Down ${Math.round(d)} spots in FantasyPros consensus (30d)\">▼${Math.round(d)}</span>`;\n}\n/* FantasyPros ECR label for a player object carrying fpEcr/fpDelta */\nfunction fpEcrLine(p, prefix){\n  if(p.fpEcr==null) return '';\n  return `<div class=\"psub\">${prefix||'FP ECR'} #${p.fpEcr} ${fpMove(p.fpDelta)}</div>`;\n}\n\n/* ---------- theme ---------- */\nfunction applyTheme(t){ if(t) document.documentElement.setAttribute('data-theme',t); }\napplyTheme(localStorage.getItem('delt_theme'));\n$('#themeBtn').onclick=()=>{\n  const cur=document.documentElement.getAttribute('data-theme');\n  const isDark = cur? cur==='dark' : matchMedia('(prefers-color-scheme:dark)').matches;\n  const next=isDark?'light':'dark'; applyTheme(next); localStorage.setItem('delt_theme',next);\n};\n\n/* ---------- header / meta ---------- */\n$('#leagueSub').innerHTML =\n  `<span class=\"tag live\">● ${esc(D.league.format)}</span>`+\n  `<span class=\"tag\">${D.league.teams} teams</span>`+\n  `<span class=\"tag\">${esc(D.league.status)}</span>`+\n  `<span class=\"tag\">${esc(D.league.lineup)}</span>`;\n$('#updated').textContent = D.updated;\n$('#nBoard').textContent = D.board.length;\n$('#nRook').textContent = D.rookies.length;\n\n/* ---------- team identity (locked) ---------- */\n$('#teamBadge').innerHTML = `<span class=\"tn\">${esc(D.myTeamName)}</span><span class=\"to\">@${esc(D.myOwner)}</span>`;\nconst myId = ()=> MY;\nfunction ownLabel(oid){ // returns {cls,text} — rival identities intentionally not shown\n  if(oid==null) return {cls:'fa',text:'AVAILABLE'};\n  if(oid===MY) return {cls:'mine',text:'YOUR TEAM'};\n  return {cls:'',text:'ROSTERED'};\n}\n\n/* ---------- movers ---------- */\nfunction renderMovers(){\n  const groups={up:[],down:[],watch:[]};\n  D.movers.forEach(m=>groups[m.direction]?.push(m));\n  const titles={up:'Rising',down:'Falling',watch:'Watch'};\n  $('#movers').innerHTML = ['up','down','watch'].map(dir=>{\n    const items=groups[dir].map(m=>{\n      const o=ownLabel(m.owner);\n      return `<div class=\"mitem\"><div class=\"pl\"><span class=\"nm\">${esc(m.name)}</span>\n        <span class=\"own ${o.cls}\" title=\"${esc(o.text)}\">${o.cls==='mine'?'YOURS':o.cls==='fa'?'FA':'ROSTERED'}</span></div>\n        <div class=\"meta\">${m.pos} · ${esc(m.team)}</div>\n        <div class=\"txt\">${esc(m.note)}</div></div>`;\n    }).join('');\n    return `<div class=\"mcol ${dir}\"><h3><span class=\"dot\"></span>${titles[dir]} <span class=\"mono\" style=\"color:var(--faint);font-weight:400;margin-left:auto;font-size:11px;\">${groups[dir].length}</span></h3>${items}</div>`;\n  }).join('');\n}\n\n/* ---------- board ---------- */\nfunction posChips(id,cur,cb){\n  const el=$(id);\n  el.innerHTML=['ALL','QB','RB','WR','TE'].map(p=>`<button class=\"chip ${p===cur?'on':''}\" data-p=\"${p}\">${p}</button>`).join('');\n  el.querySelectorAll('.chip').forEach(c=>c.onclick=()=>cb(c.dataset.p));\n}\nfunction tierPill(t){return `<span class=\"pill ${TIER_CLS[t]}\">${t}</span>`;}\nfunction posTag(p){return `<span class=\"pos ${p}\">${p}</span>`;}\n\nfunction renderBoard(){\n  let rows=D.board.slice();\n  if(state.pos!=='ALL') rows=rows.filter(r=>r.pos===state.pos);\n  if(state.sent!=='ALL') rows=rows.filter(r=>(r.sent&&r.sent.signal)===state.sent);\n  if(state.avail) rows=rows.filter(r=>r.owner==null);\n  else if(!state.scout) rows=rows.filter(r=>r.owner==null || r.owner===MY); // default: your players + available only\n  if(state.search){const q=state.search.toLowerCase();rows=rows.filter(r=>(r.name+' '+r.team+' '+r.note+' '+(r.sent&&r.sent.take||'')).toLowerCase().includes(q));}\n  const k=state.sortK,dir=state.sortDir;\n  rows.sort((a,b)=>{\n    let av,bv;\n    if(k==='tier'){av=TIER_ORD[a.tier];bv=TIER_ORD[b.tier];}\n    else if(k==='sent'){av=a.sent?SIG_ORD[a.sent.signal]:3;bv=b.sent?SIG_ORD[b.sent.signal]:3;}\n    else if(k==='owner'){av=a.owner==null?-1:(a.owner===myId()?0:1);bv=b.owner==null?-1:(b.owner===myId()?0:1);}\n    else {av=a[k];bv=b[k];}\n    if(av<bv)return -1*dir; if(av>bv)return 1*dir;\n    return a.rank-b.rank;\n  });\n  $('#boardBody').innerHTML = rows.length? rows.map(r=>{\n    const o=ownLabel(r.owner);\n    const flag=/CAMP FLAG|possible sell|downside|slid behind|cratered/i.test(r.note)?'<span class=\"flag\">WATCH</span>':'';\n    return `<tr class=\"${r.owner===myId()?'mine':''}\">\n      <td class=\"rk\">${r.rank}</td>\n      <td><span class=\"pname\">${esc(r.name)}</span>${flag}${fpEcrLine(r)}</td>\n      <td>${posTag(r.pos)}</td>\n      <td class=\"psub\">${esc(r.team)}</td>\n      <td class=\"age\">${r.age??'—'}</td>\n      <td>${tierPill(r.tier)}</td>\n      <td>${sigPill(r.sent)}</td>\n      <td><span class=\"ownfull ${o.cls}\" title=\"${esc(o.text)}\">${esc(o.text)}</span></td>\n      <td class=\"note\">${esc(r.note)}</td></tr>`;\n  }).join('') : `<tr><td colspan=\"9\" class=\"empty\">No players match these filters.</td></tr>`;\n  $('#boardCount').textContent = `${rows.length} shown · your players (gold) + available · toggle \"Include rostered\" to scout the league`;\n  $$('#boardTbl thead th').forEach(th=>{const ar=th.querySelector('.ar');if(ar)ar.textContent=th.dataset.k===state.sortK?(state.sortDir>0?'▲':'▼'):'';});\n}\n\n/* ---------- rookie PICK PLANNER (driven by actual owned picks, post-trade) ---------- */\nfunction renderPlanner(){\n  const sorted=D.rookies.filter(r=>!r.drafted).slice().sort((a,b)=>a.rank-b.rank); // drop already-drafted\n  const needs=new Set(D.myNeeds||[]);\n  const picks=(D.myPicks||[]).slice().sort((a,b)=>a.overall-b.overall);\n  const needTxt = needs.size?`<b style=\"color:var(--down)\">roster needs: ${[...needs].join(', ')}</b> (blue badges flag fits) · `:'';\n  const acquired = picks.filter(p=>p.fromRoster!==D.myTeam).length;\n  const dstat = D.draftStatus==='complete'?'<span class=\"tag\">Draft complete</span>'\n    : D.draftStatus==='drafting'?`<span class=\"tag live\" style=\"color:var(--down);border-color:var(--down);\">● DRAFT LIVE — ${D.draftMade}/48 in</span>`\n    : '<span class=\"tag\">Pre-draft</span>';\n  $('#plannerMeta').innerHTML=`${dstat} &nbsp; ${needTxt}${picks.length} pick${picks.length!==1?'s':''} from Sleeper (post-trade): <b>${picks.map(p=>p.label).join(', ')||'none'}</b>${acquired?` · ${acquired} acquired via trade`:''}${D.draftMade?` · <b>${D.draftMade}</b> off the board`:''}`;\n  let html='';\n  if(!picks.length){ $('#plannerCards').innerHTML='<div class=\"empty\" style=\"padding:24px\">You currently hold no rookie picks in this draft (all traded away).</div>'; return; }\n  for(const pk of picks){\n    const P=pk.overall, label=pk.label;\n    const deep = P > sorted.length;   // pick is past the ranked board\n    const target=sorted.find(x=>x.rank>=P) || sorted[sorted.length-1];\n    let cands=sorted.filter(x=>x.rank>=P-3 && x.rank<=P+5);\n    if(!cands.length) cands=sorted.slice(-5);   // deep pick: show the best names that could slip this far\n    const rows=cands.map(x=>{\n      const rel = x.rank<P?'fall':(x.rank<=P+2?'value':'reach');\n      const relLab = x.rank<P?'could fall':(x.rank<=P+2?'value':'reach');\n      const fit = needs.has(x.pos)?`<span class=\"fitbadge\" title=\"You're thin at ${x.pos}\">FILLS ${x.pos}</span>`:'';\n      const star = (target&&x.name===target.name)?'★ ':'';\n      return `<div class=\"pcand ${rel}\"><span class=\"pcrank\">#${x.rank}</span>\n        <span class=\"pcname\">${star}${esc(x.name)}</span> ${posTag(x.pos)}\n        <span class=\"pcslot\">${esc(x.rookieRound)}</span> ${sigPill(x.sent)}\n        ${fit}<span class=\"pcrel\">${relLab}</span></div>`;\n    }).join('');\n    html+=`<div class=\"pcard\">\n      <div class=\"phead\"><span class=\"ppick\">${label}</span><span class=\"poverall\">overall #${P}</span></div>\n      <div class=\"ptarget\">🎯 Target: <b>${target?esc(target.name):'BPA'}</b>${target?` <span class=\"psub\">${target.pos} · ${esc(target.team)} · aim ${esc(target.rookieRound)}</span>`:''}</div>\n      ${deep?'<div class=\"needline\" style=\"margin:0 14px 4px;font-size:11.5px\">Past the ranked board — most consensus rookies are gone by here; swing for upside/handcuff fliers who could slip.</div>':''}\n      <div class=\"pcands\">${rows||'<div class=\"empty\" style=\"padding:12px\">Deep pick — take best player available or a high-upside stash.</div>'}</div>\n    </div>`;\n  }\n  $('#plannerCards').innerHTML=html;\n}\n\n/* ---------- analyst take (FantasyPros news + consensus + dynasty buzz) ---------- */\nfunction fpNewsLine(r){\n  if(!r.news || !(r.news.impact||r.news.title)) return '';\n  const link=r.news.link?` <a href=\"${esc(r.news.link)}\" target=\"_blank\" rel=\"noopener\" style=\"color:var(--gold-ink);text-decoration:none;\">↗</a>`:'';\n  return `<div class=\"rbuzz\"><b>FantasyPros${r.news.date?' · '+esc(r.news.date):''}.</b> ${esc(r.news.title||'')}${r.news.impact?' — '+esc(r.news.impact):''}${link}</div>`;\n}\nfunction rangeTxt(r){ return r.ecrRange?`<span class=\"psub\">analysts #${r.ecrRange.min}–#${r.ecrRange.max}</span>`:''; }\n/* daily LLM-researched current analyst read (freshest voice) */\nfunction freshTakeLine(r){ return r.freshTake?`<div class=\"rbuzz\" style=\"border-left:2px solid var(--gold-line);padding-left:8px;\"><b class=\"hl\">Analyst read.</b> ${esc(r.freshTake)}</div>`:''; }\n\n/* ---------- best available (live draft board) ---------- */\nfunction renderBPA(){\n  const needs=new Set(D.myNeeds||[]);\n  const left=D.rookies.filter(r=>!r.drafted).sort((a,b)=>a.rank-b.rank);\n  $('#bpaCount').textContent=left.length+' left';\n  const myNext=(D.myPicks||[]).map(p=>p.overall).sort((a,b)=>a-b).find(o=>o>(D.draftMade||0));\n  $('#bpaNote').innerHTML = (D.draftStatus==='drafting'\n      ? `<span class=\"hl\">● Draft live</span> — ${D.draftMade}/48 gone. `\n      : '') + (myNext?`Your next pick is overall <b>#${myNext}</b>. `:'') +\n    `Analyst take = <span class=\"hl\">FantasyPros</span> news + consensus, plus dynasty buzz.`;\n  $('#bpaList').innerHTML = left.slice(0,8).map((r,i)=>{\n    const pc=PROF_CLS[r.profile]||'p-bal';\n    const need=needs.has(r.pos)?` <span class=\"fitbadge\">FILLS ${r.pos}</span>`:'';\n    const yours=(myNext&&r.fpEcr)?'':''; // (reserved)\n    const ceiling=r.ceiling?`<div class=\"cf up\"><span class=\"cfi\">▲</span><span class=\"cft\">${esc(r.ceiling)}</span></div>`:'';\n    const floor=r.floor?`<div class=\"cf dn\"><span class=\"cfi\">▼</span><span class=\"cft\">${esc(r.floor)}</span></div>`:'';\n    const buzz=(r.buzz||r.note)?`<div class=\"rbuzz\"><b>Dynasty buzz.</b> ${esc(r.buzz||r.note)}</div>`:'';\n    return `<div class=\"rcard ${pc}\">\n      <div class=\"rtop\"><div>\n        <div class=\"rname\">${i===0?'★ ':''}${esc(r.name)}${need}</div>\n        <div class=\"rmeta\">${posTag(r.pos)} ${esc(r.team)}${r.college?' · '+esc(r.college):''} · <span class=\"hl\">ECR #${r.fpEcr??'—'}</span> ${rangeTxt(r)} ${sigPill(r.sent)}</div>\n      </div><div class=\"rpick\"><div class=\"rr\">${esc(r.rookieRound)}</div>${r.tier?`<div class=\"rt\">${esc(r.tier)}</div>`:''}</div></div>\n      ${freshTakeLine(r)}${fpNewsLine(r)}${buzz}${ceiling}${floor}${yours}</div>`;\n  }).join('') || '<div class=\"empty\">Draft complete — the board is empty.</div>';\n}\n\n/* ---------- rookies (draft war room) ---------- */\nfunction renderRookies(){\n  let rows=D.rookies.slice();\n  if(state.rpos!=='ALL') rows=rows.filter(r=>r.pos===state.rpos);\n  if(state.rprof!=='ALL') rows=rows.filter(r=>(r.profile||'')===state.rprof);\n  if(state.rsearch){const q=state.rsearch.toLowerCase();rows=rows.filter(r=>(r.name+' '+r.team+' '+r.college+' '+(r.buzz||r.note||'')+' '+(r.comp||'')).toLowerCase().includes(q));}\n  const grid=$('#rookGrid');\n  if(!rows.length){grid.innerHTML='<div class=\"empty\">No rookies match these filters.</div>';return;}\n  grid.innerHTML = rows.map((r,i)=>{\n    const pc=PROF_CLS[r.profile]||'p-bal';\n    const ceiling=r.ceiling?`<div class=\"cf up\"><span class=\"cfi\">▲</span><span class=\"cft\">${esc(r.ceiling)}</span></div>`:'';\n    const floor=r.floor?`<div class=\"cf dn\"><span class=\"cfi\">▼</span><span class=\"cft\">${esc(r.floor)}</span></div>`:'';\n    const buzz=(r.buzz||r.note)?`<div class=\"rbuzz\"><b>Buzz.</b> ${esc(r.buzz||r.note)}</div>`:'';\n    const comp=r.comp?`<div class=\"rcomp\">comp: ${esc(r.comp)}</div>`:'';\n    const prof=r.profile?`<span class=\"prof ${pc}\">${esc(r.profile)}</span>`:'';\n    const tier=r.tier?`<div class=\"rt\">${esc(r.tier)}</div>`:'';\n    const sent=r.sent?`<div class=\"rsent\"><span class=\"lab\">Analyst</span>${sigPill(r.sent)}</div>`:'';\n    return `<div class=\"rcard ${pc}\" style=\"${r.drafted?'opacity:.5;':''}\">\n      <div class=\"rtop\">\n        <div><div class=\"rname\">${esc(r.name)}${r.drafted?' <span class=\"flag\">DRAFTED</span>':''}</div>\n          <div class=\"rmeta\">${posTag(r.pos)} &nbsp;${esc(r.team)} · ${esc(r.college)}${r.fpEcr!=null?` · <span class=\"hl\">FP ECR #${r.fpEcr}</span> ${fpMove(r.fpDelta)}`:''}</div></div>\n        <div class=\"rpick\"><div class=\"rr\">${esc(r.rookieRound)}</div>${tier}</div>\n      </div>\n      <div style=\"display:flex;gap:8px;align-items:center;flex-wrap:wrap;\">${prof}${sent}</div>\n      ${ceiling}${floor}\n      ${freshTakeLine(r)}${fpNewsLine(r)}${buzz}${comp}\n    </div>`;\n  }).join('');\n}\n\n/* ---------- this week ---------- */\nfunction renderWeek(){\n  const id=myId();\n  if(id==null){\n    $('#weekStart').innerHTML=$('#weekWaiver').innerHTML=`<div class=\"banner\"><p>Pick <b>your team</b> up top to load your roster trends and tailored waiver targets.</p></div>`;\n    return;\n  }\n  const roster=new Set(teamsById[id].players.map(p=>norm(p.name)));\n  // phase-aware banner\n  const inSeason = D.nfl && D.nfl.inSeason;\n  $('#weekBanner').innerHTML = inSeason\n    ? `<h3>Week ${D.nfl.week} — in season</h3><p>Your rostered players trending in the league feed (left) and the best <b>available adds</b> by dynasty value + who the league is grabbing (right). Start/sit projections layer in as the week develops.</p>`\n    : `<h3>Offseason mode — season starts soon</h3><p>Once Week 1 kicks off this becomes your weekly brain: start/sit + waivers. For now: who on your roster is trending, and the hottest <b>available</b> adds across Sleeper you can still stash.</p>`;\n  // trending: movers that are on my roster\n  const mine=D.movers.filter(m=>roster.has(norm(m.name)));\n  $('#weekStart').innerHTML = mine.length? mine.map(m=>{\n    const c=m.direction; const lab={up:'RISING',down:'FALLING',watch:'WATCH'}[c];\n    return `<div class=\"mcol ${c}\" style=\"margin-bottom:10px;\"><div class=\"mitem\" style=\"border-top:0;\">\n      <div class=\"pl\"><span class=\"nm\">${esc(m.name)} <span class=\"psub\">${m.pos}·${esc(m.team)}</span></span>\n      <span class=\"own ${c==='up'?'fa':c==='mine'?'mine':''}\" style=\"color:var(--${c});border-color:color-mix(in srgb,var(--${c}) 40%,var(--border));background:var(--${c}-soft);\">${lab}</span></div>\n      <div class=\"txt\">${esc(m.note)}</div></div></div>`;\n  }).join('') : `<div class=\"banner\"><p>None of your rostered players are in this week&rsquo;s movers feed — quiet is good. In-season, hot/cold streaks and injury news land here.</p></div>`;\n  // waivers: available vets + top available board players\n  const availBoard=D.board.filter(b=>b.owner==null).slice(0,6);\n  const vets=D.vets.slice(0,10);\n  // trending league-wide AND unrostered in your league = grab-now waiver adds\n  const hotAvail=D.movers.filter(m=>m.direction==='up' && m.owner==null).slice(0,6);\n  const hotBlock = hotAvail.length? `<div style=\"margin-bottom:12px;\"><div class=\"clab\" style=\"margin-bottom:6px;\">🔥 Trending &amp; available in your league</div>`+\n    hotAvail.map(m=>`<div class=\"mitem up\" style=\"border-left:2px solid var(--up);border-top:1px solid var(--border2);\"><div class=\"pl\"><span class=\"nm\">${esc(m.name)} <span class=\"psub\">${m.pos}·${esc(m.team)}</span></span><span class=\"own fa\">FA${m.fpEcr!=null?' · ECR #'+m.fpEcr:''}</span></div><div class=\"txt\">${esc(m.note)}</div></div>`).join('')+`</div>`:'';\n  $('#weekWaiver').innerHTML = hotBlock +\n    `<div class=\"banner\" style=\"margin-bottom:10px;\"><p><b>Available now</b>, ranked by <span class=\"hl\">FantasyPros dynasty ECR</span> — the best unrostered players by real analyst consensus (lower # = higher value). In-season this folds in weekly projections + trending adds.</p></div>`+\n    `<div class=\"tablewrap\"><table style=\"min-width:auto;\"><thead><tr><th>FP ECR</th><th>Player</th><th>Pos</th><th>NFL</th><th>Age</th></tr></thead><tbody>`+\n    vets.map(v=>`<tr><td class=\"rk\">${v.fpEcr!=null?'#'+v.fpEcr:'—'}</td><td class=\"pname\">${esc(v.name)}${v.fpDelta!=null?' '+fpMove(v.fpDelta):''}</td><td>${posTag(v.pos)}</td><td class=\"psub\">${esc(v.team)}</td><td class=\"age\">${v.age??'—'}</td></tr>`).join('')+\n    `</tbody></table></div>`;\n}\n\n/* ---------- my roster ---------- */\nfunction renderRoster(){\n  const id=myId();\n  if(id==null){$('#rosterSection').innerHTML=`<div class=\"banner\"><h3>Select your team</h3><p>Choose your team in the header to see roster construction, age curve, and positional needs.</p></div>`;return;}\n  const t=teamsById[id];\n  const byPos={QB:[],RB:[],WR:[],TE:[]};\n  t.players.forEach(p=>{if(byPos[p.pos])byPos[p.pos].push(p);});\n  const boardNames=new Set(D.board.map(b=>norm(b.name)));\n  const onBoard=p=>boardNames.has(norm(p.name));\n  const avg=arr=>arr.length?Math.round(arr.reduce((s,p)=>s+(p.age||0),0)/arr.filter(p=>p.age).length):0;\n  // superflex-aware need scoring: want depth at QB (2 start) + RB/WR\n  const targets={QB:3,RB:5,WR:6,TE:2};\n  const cards=['QB','RB','WR','TE'].map(pos=>{\n    const arr=byPos[pos], nb=arr.filter(onBoard).length, tv=targets[pos];\n    const health=Math.min(1,(nb/tv));\n    const col=health>=.75?'var(--up)':health>=.4?'var(--gold)':'var(--down)';\n    return `<div class=\"card\"><div class=\"lab\">${pos}</div>\n      <div class=\"big\">${arr.length}<small> / ${nb} ranked</small></div>\n      <div class=\"sub2\">avg age ${avg(arr)||'—'}</div>\n      <div class=\"needbar\"><i style=\"width:${Math.round(health*100)}%;background:${col};\"></i></div></div>`;\n  }).join('');\n  const listFor=pos=>byPos[pos].slice().sort((a,b)=>((a.fpEcr??9999)-(b.fpEcr??9999))||(onBoard(b)-onBoard(a))||((a.age||99)-(b.age||99)))\n    .map(p=>`<tr><td class=\"rk\">${p.fpEcr!=null?'#'+p.fpEcr:''}</td><td><span class=\"pname\">${esc(p.name)}</span>${onBoard(p)?' <span class=\"flag\" style=\"color:var(--gold-ink);border-color:var(--gold-line);background:var(--gold-soft);\">TOP-100</span>':''}${p.fpDelta!=null?' '+fpMove(p.fpDelta):''}</td><td class=\"psub\">${esc(p.team)}</td><td class=\"age\">${p.age??'—'}</td></tr>`).join('');\n  const totalRanked=t.players.filter(onBoard).length;\n  $('#rosterSection').innerHTML=`\n    <div class=\"sechead\"><h2>${esc(t.name)}</h2><span class=\"note\">${t.players.length} players · ${totalRanked} in dynasty top-100 · owner ${esc(t.owner)}</span></div>\n    <div class=\"cards\" style=\"margin-bottom:8px;\">${cards}</div>\n    <p class=\"note\" style=\"margin:4px 2px 18px;\">Bars show how many rostered players crack the dynasty top-100 vs. a healthy superflex target (QB 3 / RB 5 / WR 6 / TE 2). Red = thin, worth targeting in trades or the rookie draft.</p>\n    <div class=\"two\">\n      ${['QB','RB','WR','TE'].map(pos=>`<div><div class=\"sechead\" style=\"margin-bottom:8px;\"><h2>${pos} <span class=\"mono\" style=\"color:var(--faint);font-size:11px;\">${byPos[pos].length}</span></h2></div>\n      <div class=\"tablewrap\"><table style=\"min-width:auto;\"><tbody>${listFor(pos)||'<tr><td class=\"empty\">none</td></tr>'}</tbody></table></div></div>`).join('')}\n    </div>`;\n}\n\n/* ---------- trades ---------- */\nlet trade = { give: [], get: [] };\nfunction assetPool(){\n  const pool=[], seen=new Set();\n  (D.allTeams||[]).forEach(t=>t.players.forEach(p=>{\n    const id='pl:'+norm(p.name); if(seen.has(id))return; seen.add(id);\n    pool.push({id,name:p.name,pos:p.pos,team:p.team,value:p.value||0,fpEcr:p.fpEcr,ownerId:t.id,ownerName:t.name});\n  }));\n  (D.myPicks||[]).forEach(pk=>pool.push({id:'pk:'+pk.label,name:'Pick '+pk.label,pos:'PICK',team:'—',value:pk.value||0,ownerId:D.myTeam,ownerName:'You'}));\n  return pool;\n}\nconst POOL = assetPool(); const POOL_BY_LABEL = {};\nfunction assetLabel(a){ return a.pos==='PICK'? a.name+' (rookie pick)' : `${a.name} — ${a.pos} · ${a.ownerName}`; }\nfunction buildDatalist(){\n  POOL.forEach(a=>POOL_BY_LABEL[assetLabel(a)]=a);\n  $('#assetList').innerHTML = POOL.slice().sort((x,y)=>y.value-x.value).map(a=>`<option value=\"${esc(assetLabel(a))}\"></option>`).join('');\n}\nfunction renderTradeSide(side){\n  const el=$('#tside_'+side), items=trade[side];\n  el.querySelector('.tsum').textContent=items.reduce((s,a)=>s+a.value,0).toLocaleString();\n  el.querySelector('.tlist').innerHTML = items.length? items.map((a,i)=>\n    `<div class=\"mitem\" style=\"display:flex;justify-content:space-between;gap:8px;align-items:center;border-top:1px solid var(--border2);\">\n      <span><span class=\"pname\">${esc(a.name)}</span> ${a.pos!=='PICK'?posTag(a.pos):'<span class=\"pill t1\">PICK</span>'} <span class=\"psub\">${esc(a.ownerName)}</span></span>\n      <span style=\"display:flex;gap:8px;align-items:center;\"><b class=\"mono\">${a.value.toLocaleString()}</b>\n      <button class=\"chip tdel\" data-side=\"${side}\" data-i=\"${i}\" style=\"padding:2px 8px;\">✕</button></span></div>`).join('')\n    : '<div class=\"empty\" style=\"padding:12px\">Add players or picks…</div>';\n  el.querySelectorAll('.tdel').forEach(b=>b.onclick=()=>{trade[b.dataset.side].splice(+b.dataset.i,1);renderTradeVerdict();});\n}\nfunction renderTradeVerdict(){\n  renderTradeSide('give'); renderTradeSide('get');\n  const g=trade.give.reduce((s,a)=>s+a.value,0), r=trade.get.reduce((s,a)=>s+a.value,0), v=$('#tradeVerdict');\n  if(!trade.give.length && !trade.get.length){ v.innerHTML='<p>Add assets to both sides to see the verdict.</p>'; v.style.borderLeftColor='var(--gold)'; return; }\n  const diff=r-g, tot=Math.max(g,r,1), pct=Math.round(Math.abs(diff)/tot*100);\n  let col,txt;\n  if(pct<=5){col='var(--gold)';txt=`<b>Fair trade.</b> Within ${pct}% — ${g.toLocaleString()} given vs ${r.toLocaleString()} received.`;}\n  else if(diff>0){col='var(--up)';txt=`<b style=\"color:var(--up)\">You win this by ${pct}%.</b> Receive ${r.toLocaleString()} for ${g.toLocaleString()} — net +${diff.toLocaleString()}.`;}\n  else {col='var(--down)';txt=`<b style=\"color:var(--down)\">You lose ${pct}% of value.</b> Give ${g.toLocaleString()} for ${r.toLocaleString()} — net ${diff.toLocaleString()}.`;}\n  v.innerHTML=`<p>${txt}</p>`; v.style.borderLeftColor=col;\n}\nfunction wireTradeAdds(){\n  $$('.tadd').forEach(inp=>inp.onchange=()=>{ const a=POOL_BY_LABEL[inp.value]; if(a){ trade[inp.dataset.side].push(a); inp.value=''; renderTradeVerdict(); } });\n}\nfunction renderTargets(){\n  const needs=new Set(D.myNeeds||[]); let rows=[];\n  (D.allTeams||[]).forEach(t=>{ if(t.id===D.myTeam)return; t.players.forEach(p=>rows.push({...p,ownerName:t.name})); });\n  const pf=state.ttPos||'ALL';\n  rows = pf!=='ALL'? rows.filter(r=>r.pos===pf) : rows.filter(r=>['QB','RB','WR','TE'].includes(r.pos));\n  if(state.ttSearch){const q=state.ttSearch.toLowerCase();rows=rows.filter(r=>(r.name+' '+r.team+' '+r.ownerName).toLowerCase().includes(q));}\n  rows.sort((a,b)=>(b.value||0)-(a.value||0)); rows=rows.slice(0,40);\n  $('#ttBody').innerHTML = rows.length? rows.map(r=>{\n    const need=needs.has(r.pos)?' <span class=\"fitbadge\">NEED</span>':'';\n    return `<tr><td class=\"rk\">${(r.value||0).toLocaleString()}</td>\n      <td><span class=\"pname\">${esc(r.name)}</span>${need}${r.fpEcr!=null?`<div class=\"psub\">ECR #${r.fpEcr}</div>`:''}</td>\n      <td>${posTag(r.pos)}</td><td class=\"psub\">${esc(r.team)}</td><td class=\"ownfull\">${esc(r.ownerName)}</td></tr>`;\n  }).join('') : `<tr><td colspan=\"5\" class=\"empty\">No targets match.</td></tr>`;\n}\nfunction renderTTChips(){ posChips('#ttPosChips', state.ttPos||'ALL', p=>{state.ttPos=p;renderTTChips();renderTargets();}); }\nfunction renderTrades(){ buildDatalist(); wireTradeAdds(); renderTradeVerdict(); renderTTChips(); renderTargets(); }\n\n/* ---------- name normalize (mirror of python) ---------- */\nfunction norm(n){return n.toLowerCase().replace(/[.'’]/g,'').replace(/\\b(jr|sr|ii|iii|iv|v)\\b\\.?/g,'').replace(/[^a-z ]/g,'').replace(/\\s+/g,' ').trim();}\n\n/* ---------- wire up ---------- */\nfunction chipRow(id,opts,cur,cb){const el=$(id);el.innerHTML=opts.map(o=>`<button class=\"chip ${o===cur?'on':''}\" data-v=\"${esc(o)}\">${esc(o)}</button>`).join('');el.querySelectorAll('.chip').forEach(c=>c.onclick=()=>cb(c.dataset.v));}\nfunction renderBoardChips(){posChips('#posChips',state.pos,p=>{state.pos=p;renderBoardChips();renderBoard();});}\nfunction renderSentChips(){chipRow('#sentChips',['ALL','Buy','Hold','Sell'],state.sent,v=>{state.sent=v;renderSentChips();renderBoard();});}\nfunction renderRookChips(){posChips('#rposChips',state.rpos,p=>{state.rpos=p;renderRookChips();renderRookies();});}\nfunction renderProfChips(){chipRow('#rprofChips',['ALL','Boom','High-variance','Balanced','Safe floor','Bust risk'],state.rprof,v=>{state.rprof=v;renderProfChips();renderRookies();});}\nrenderBoardChips(); renderSentChips(); renderRookChips(); renderProfChips();\n$('#search').oninput=e=>{state.search=e.target.value;renderBoard();};\n$('#rsearch').oninput=e=>{state.rsearch=e.target.value;renderRookies();};\n$('#ttSearch').oninput=e=>{state.ttSearch=e.target.value;renderTargets();};\n$('#tAvail').onchange=e=>{state.avail=e.target.checked;renderBoard();};\n$('#tScout').onchange=e=>{state.scout=e.target.checked;renderBoard();};\n$$('#boardTbl thead th.sortable').forEach(th=>th.onclick=()=>{\n  const k=th.dataset.k;\n  if(state.sortK===k)state.sortDir*=-1; else{state.sortK=k;state.sortDir=(k==='age'||k==='rank')?1:1;}\n  renderBoard();\n});\n$$('#tabs .tab').forEach(tb=>tb.onclick=()=>{\n  $$('#tabs .tab').forEach(x=>x.classList.remove('active'));tb.classList.add('active');\n  $$('.view').forEach(v=>v.classList.remove('active'));$('#view-'+tb.dataset.view).classList.add('active');\n});\n\nfunction renderAll(){renderMovers();renderBoard();renderBPA();renderPlanner();renderRookies();renderTrades();renderWeek();renderRoster();}\nrenderAll();\n</script>\n";
